# mysql-library-rental-analytics
A complete MySQL portfolio project covering database design, CRUD operations, joins, filtering, set operations, subqueries, aggregate reporting, transactions, indexes, views, metadata queries, and window functions.
